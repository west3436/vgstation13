// Hand-curated overmap body catalog. Phase 1 leaves this empty — Phase 1's two
// test bodies are placed inline in populate_grid.dm. Phase 4 introduces the
// Odyssey homebase + NT outpost + Centcomm + Vox bazaar definitions here.
// Phase 6 adds shuttle-specific bodies (mining asteroid, syndicate station,
// trade hub, ...).
